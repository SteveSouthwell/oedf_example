rem ###################################################################
rem # Basic gradle wrapper for progradle, which is itself a wrapper.
rem # This file just gets Jenkins in the right location to do the build.
rem ########################################################################

call %DLC%\bin\progradle %*
